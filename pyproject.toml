[project]
name = "zdrowskit"
version = "0.1.0"
description = "What Apple Health notifications should be."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "litellm",
    "markdown",
    "python-dotenv",
    "resend",
    "rich",
    "watchdog",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
